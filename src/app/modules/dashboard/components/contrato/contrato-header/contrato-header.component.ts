import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';
import { Router } from 'express';

@Component({
    selector: 'db-contrato-header',
    templateUrl: './contrato-header.component.html',
    standalone: true,
    imports: [RouterModule]
})
export class ContratoHeaderComponent {

}
